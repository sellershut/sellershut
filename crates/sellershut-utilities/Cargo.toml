[package]
name = "sellershut-utilities"
version = "0.0.0"
edition = "2024"
license.workspace = true
publish = false

[dependencies]
base64.workspace = true
rand.workspace = true
redis = { workspace = true, optional = true }
sha2.workspace = true

[features]
default = []
cache-key = ["dep:redis"]
