[package]
name = "categories-service"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
sellershut-core = { workspace = true, features = ["rpc-server-categories","serde", "time"] }
